# GitHub Automation Module

## Responsibility

`.github/` owns repository automation for hosted demos and CI-style delivery.

## Members

- `workflows/`: GitHub Actions workflow definitions.

## Boundaries

- Keep app runtime code in `src/`.
- Keep static assets in `public/`.
- Keep visual evidence in `eval/`.
