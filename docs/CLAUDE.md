# Documentation Assets Module

## Responsibility

`docs/` owns public documentation media that must render directly on GitHub.

## Members

- `demo.gif`: Animated README demo generated from real app states.

## Boundaries

- Keep temporary capture frames out of this directory.
- Keep validation screenshots in `eval/screenshots/`.
- Keep app runtime assets in `public/`.
