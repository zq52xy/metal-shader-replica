# Scripts Module

## Responsibility

`scripts/` owns local helper scripts for running and validating the shader replica.

## Members

- `start-dev.ps1`: Starts the Vite dev server on `http://127.0.0.1:5173/`.

## Boundaries

- Keep application behavior in `src/`.
- Keep evidence and reports in `eval/`.
